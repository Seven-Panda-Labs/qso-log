title = "qso-log gitleaks"

# Bump when the pinned version in .github/workflows/secret-scan.yml changes.
minVersion = "8.30.1"

[extend]
useDefault = true

[[allowlists]]
description = "Placeholder env templates and lockfiles"
paths = [
  '''(?i)\.env\.example$''',
  '''(?i)package-lock\.json$''',
]
